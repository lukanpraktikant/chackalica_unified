# Generated by Django 5.2.15 on 2026-07-13 18:55

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('training', '0010_evalrun_score_threshold'),
    ]

    operations = [
        migrations.AddField(
            model_name='experiment',
            name='best_metric',
            field=models.CharField(choices=[('map50', 'map50'), ('map50_95', 'map50_95'), ('precision', 'precision'), ('recall', 'recall'), ('f1', 'f1'), ('f1+map50', 'f1+map50 (average of both)')], default='map50', help_text="Validation metric that selects each run's best checkpoint and drives early stopping. precision/recall/f1 are computed at the eval score threshold; f1+map50 tracks the average of both.", max_length=32),
        ),
        migrations.AddField(
            model_name='experiment',
            name='val_interval',
            field=models.PositiveIntegerField(default=1, help_text='Run validation every N epochs (the final epoch always validates). 1 = every epoch. Early-stopping patience counts scored epochs only, so patience 3 with interval 5 spans 15 train epochs.'),
        ),
        migrations.AlterField(
            model_name='experiment',
            name='early_stopping_patience',
            field=models.PositiveIntegerField(blank=True, help_text="Stop a model's training after this many scored epochs with no best-metric improvement (the best checkpoint is kept). Blank = train all epochs. Needs a val dataset.", null=True),
        ),
    ]
